name: E2E

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - edited

jobs:
  e2e-default:
    runs-on: ubuntu-20.04

    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set labels
        run: ./src/labels/set.sh
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_ISSUE_NUMBER: ${{ github.event.number }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          LABELS: "type/fix,type/feat,component/embeds,component/auth,component/docs"

      - name: Test Run
        id: test-run
        uses: ./
        with:
          github_pr_title: "fix(auth, embeds): test 123 mne grustno v shkole nevkusnaya kapusta"

      - name: Get labels
        id: get-labels
        run: ./src/labels/get.sh
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_ISSUE_NUMBER: ${{ github.event.number }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Assert placeholder
        uses: nick-fields/assert-action@v2
        with:
          actual: ${{ steps.get-labels.outputs.labels }}
          expected: "type/fix,component/auth,component/embeds"

      - name: Delete all labels
        run: ./src/labels/delete_all.sh
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_ISSUE_NUMBER: ${{ github.event.number }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
